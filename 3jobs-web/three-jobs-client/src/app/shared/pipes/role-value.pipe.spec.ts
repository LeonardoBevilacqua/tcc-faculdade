import { RoleValuePipe } from './role-value.pipe';

describe('RoleValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleValuePipe();
    expect(pipe).toBeTruthy();
  });
});
